eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
fetch-registry: true
register-with-eureka: true
spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888